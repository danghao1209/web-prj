import DefaultLayerAccount from '../DefaultLayerAccount';
function Orders() {
    return (
        <DefaultLayerAccount>
            <div>Orders</div>
        </DefaultLayerAccount>
    );
}

export default Orders;
