import DefaultLayerAccount from '../DefaultLayerAccount';

function Addresses() {
    return (
        <DefaultLayerAccount>
            <div>Addresses</div>
        </DefaultLayerAccount>
    );
}

export default Addresses;
